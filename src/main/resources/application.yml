spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nyangmunity?useSSL=false&serverTimezone=UTC
    username: root
    password: jdg1234
  jpa:
    hibernate:
      ddl-auto: create
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database: mysql